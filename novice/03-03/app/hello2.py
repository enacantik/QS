app = Flask(__name__)

@app.route('/')
def index(:
    return 'index'

@app.route('/login', methods=['GET', 'POST'])
def login():
    if request.method == 'POST'
        return login()

from flask import render_template

@app.route('hello/')
